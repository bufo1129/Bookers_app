<h1>New book</h1>
<% form_with @book, local:true do |f| %>
  <% render 'layouts/books_error_messages', model: f.object %>


  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>

<% end %>
